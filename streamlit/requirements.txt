# TrendPilot Streamlit App — Python dependencies
# Install: pip install -r requirements.txt
#
# Also required (system-level, not pip):
#   Graphviz binaries — https://graphviz.org/download/
#   (needed by the graphviz Python package for diagram rendering)

# ── Core Streamlit app ────────────────────────────────────────────────────────
streamlit>=1.32.0
numpy>=1.26.0
pandas>=2.0.0
joblib>=1.3.0
scikit-learn>=1.4.0          # deserialises the HGBR .pkl engagement models

# ── Module 01 — Trend Identification ─────────────────────────────────────────
openai>=1.6.0                 # GPT-4o calls (trend extraction + post titles)
pytrends>=4.9.2               # Google Trends API wrapper

# ── Module 04 — Visual Generation ────────────────────────────────────────────
requests>=2.31.0              # Alibaba DashScope image API + Ollama fallback
graphviz>=0.20.1              # Python binding for Graphviz diagram rendering

# ── Optional: Stable Diffusion local fallback (disabled by default) ───────────
# torch>=2.1.0
# diffusers>=0.25.0
