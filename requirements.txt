# Requirements for visual_generation image pipeline (CPU-ready)
# Note: install CPU PyTorch wheels on Windows using the PyTorch CPU index:
#   python -m pip install --upgrade --force-reinstall --no-cache-dir torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

torch==2.10.0+cpu
torchvision==0.25.0+cpu
torchaudio==2.10.0+cpu

# Diffusion & transformers
diffusers==0.36.0
transformers==5.1.0
accelerate==1.12.0
huggingface-hub==1.4.1
safetensors==0.7.0
tokenizers==0.22.2

# Imaging & utilities
pillow==12.1.1
numpy==2.4.2
requests==2.32.5
httpx==0.28.1

# Optional (for better memory-efficient attention if you later enable GPU/xformers)
# xformers==0.0.34

# Pin setuptools to a known good version if needed
setuptools==82.0.0

# To install everything (after activating the venv):
#   python -m pip install -r requirements.txt
# For the PyTorch CPU wheels on Windows use the index-url command shown above first, then:
#   python -m pip install --upgrade --force-reinstall --no-cache-dir -r requirements.txt
